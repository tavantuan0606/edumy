<% layout('/layouts/boilerplate')%>
<h1>All members</h1>
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>Name</th>
            <th>Age</th>
            <th>Nationality</th>
            <th>Sex</th>
            <th>Email</th>
        </tr>
    </thead>
    <% for(let par of participants) { %>
        <tr>
            <% if(par.name) {%>
            <td>
                <%= par.name %>
            </td>
            <td>
                <%= par.age %>
            </td>
            <td>
                <%= par.nationality %>
            </td>
            <td>
                <%= par.sex %>
            </td>
            <td>
                <%= par.e_mail %>
            </td>
            
            <!-- <td>
                <= par.race_result %>
                < if(par.race_result) {%>
                    m
                    < } %>
            </td> -->
            <% if(currentUser && currentUser._id == '675da20f3dfdaa4f789ffa00') {%>
            <td><a href="/members/<%=par._id%>" class="btn btn-info">Info</a></td>
            <% } %>
            <% } %>
        </tr>
        <% } %>    
</table>